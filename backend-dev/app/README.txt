Ensure Node NPM http-server package is installed, if not run 'npm install -g http-server' once NPM is installed.

Open RUN-HTTP-SERVER.bat to open HTTP Server with Caching Disabled for development purposes.

Then can access via 'http://127.0.0.1:8000/'

